# name: Deploy Staging

# on:
#   push:
#     branches: [ staging ]

# permissions:
#   contents: read

# concurrency:
#   group: deploy-staging-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   deploy:
#     name: Deploy to Staging with Kamal
#     runs-on: ubuntu-latest
#     env:
#       RAILS_ENV: production
#       DOCKER_BUILDKIT: 1
#       SITEMAP_HOST: http://aa.okemamy.com

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Ruby
#         uses: ruby/setup-ruby@v1
#         with:
#           ruby-version: .ruby-version
#           bundler: 2.7.2
#           bundler-cache: true

#       - name: Install Kamal
#         run: |
#           gem install kamal --no-document

#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Set up SSH for Kamal
#         run: |
#           install -m 700 -d ~/.ssh
#           echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
#           chmod 600 ~/.ssh/id_ed25519
#           ssh-keygen -R 157.245.162.216 || true
#           ssh-keyscan -T 15 -t rsa,ecdsa,ed25519 157.245.162.216 >> ~/.ssh/known_hosts
#           {
#             echo "Host 157.245.162.216";
#             echo "  StrictHostKeyChecking no";
#             echo "  UserKnownHostsFile=/dev/null";
#           } >> ~/.ssh/config

#       - name: Preflight secrets check
#         env:
#           RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY_STAGING || secrets.RAILS_MASTER_KEY }}
#           KAMAL_REGISTRY_PASSWORD: ${{ secrets.KAMAL_REGISTRY_PASSWORD }}
#           POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_STAGING || secrets.POSTGRES_PASSWORD }}
#           SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#           DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
#           PAYDUNYA_MASTER_KEY: ${{ secrets.PAYDUNYA_MASTER_KEY_STAGING || secrets.PAYDUNYA_MASTER_KEY }}
#           PAYDUNYA_PRIVATE_KEY: ${{ secrets.PAYDUNYA_PRIVATE_KEY_STAGING || secrets.PAYDUNYA_PRIVATE_KEY }}
#           PAYDUNYA_TOKEN: ${{ secrets.PAYDUNYA_TOKEN_STAGING || secrets.PAYDUNYA_TOKEN }}
#           PAYDUNYA_PUBLIC_KEY: ${{ secrets.PAYDUNYA_PUBLIC_KEY_STAGING || secrets.PAYDUNYA_PUBLIC_KEY }}
#           PAYDUNYA_MODE: ${{ secrets.PAYDUNYA_MODE_STAGING || secrets.PAYDUNYA_MODE }}
#           PAYDUNYA_STORE_NAME: ${{ secrets.PAYDUNYA_STORE_NAME_STAGING || secrets.PAYDUNYA_STORE_NAME }}
#           PAYDUNYA_STORE_TAGLINE: ${{ secrets.PAYDUNYA_STORE_TAGLINE_STAGING || secrets.PAYDUNYA_STORE_TAGLINE }}
#           PAYDUNYA_STORE_PHONE: ${{ secrets.PAYDUNYA_STORE_PHONE_STAGING || secrets.PAYDUNYA_STORE_PHONE }}
#           PAYDUNYA_STORE_ADDRESS: ${{ secrets.PAYDUNYA_STORE_ADDRESS_STAGING || secrets.PAYDUNYA_STORE_ADDRESS }}
#           PAYDUNYA_STORE_URL: ${{ secrets.PAYDUNYA_STORE_URL_STAGING || secrets.PAYDUNYA_STORE_URL }}
#           PAYDUNYA_STORE_LOGO: ${{ secrets.PAYDUNYA_STORE_LOGO_STAGING || secrets.PAYDUNYA_STORE_LOGO }}
#         run: |
#           set -euo pipefail
#           for key in RAILS_MASTER_KEY KAMAL_REGISTRY_PASSWORD POSTGRES_PASSWORD SSH_PRIVATE_KEY DOCKERHUB_USERNAME; do
#             if [ -z "${!key:-}" ]; then
#               echo "Missing required secret: $key" >&2
#               exit 1
#             fi
#           done
#           # PayDunya secrets are optional but recommended
#           for key in PAYDUNYA_MASTER_KEY PAYDUNYA_PRIVATE_KEY PAYDUNYA_TOKEN PAYDUNYA_PUBLIC_KEY PAYDUNYA_MODE PAYDUNYA_STORE_NAME PAYDUNYA_STORE_URL; do
#             if [ -z "${!key:-}" ]; then
#               echo "Warning: Missing PayDunya secret: $key (PayDunya integration may not work)" >&2
#             fi
#           done

#       - name: Deploy (staging)
#         env:
#           RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY_STAGING || secrets.RAILS_MASTER_KEY }}
#           KAMAL_REGISTRY_PASSWORD: ${{ secrets.KAMAL_REGISTRY_PASSWORD }}
#           POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD_STAGING || secrets.POSTGRES_PASSWORD }}
#           SITEMAP_HOST: http://aa.okemamy.com
#           # PayDunya configuration (all as secrets, with staging fallback)
#           PAYDUNYA_MASTER_KEY: ${{ secrets.PAYDUNYA_MASTER_KEY_STAGING || secrets.PAYDUNYA_MASTER_KEY }}
#           PAYDUNYA_PRIVATE_KEY: ${{ secrets.PAYDUNYA_PRIVATE_KEY_STAGING || secrets.PAYDUNYA_PRIVATE_KEY }}
#           PAYDUNYA_TOKEN: ${{ secrets.PAYDUNYA_TOKEN_STAGING || secrets.PAYDUNYA_TOKEN }}
#           PAYDUNYA_PUBLIC_KEY: ${{ secrets.PAYDUNYA_PUBLIC_KEY_STAGING || secrets.PAYDUNYA_PUBLIC_KEY }}
#           PAYDUNYA_MODE: ${{ secrets.PAYDUNYA_MODE_STAGING || secrets.PAYDUNYA_MODE }}
#           PAYDUNYA_STORE_NAME: ${{ secrets.PAYDUNYA_STORE_NAME_STAGING || secrets.PAYDUNYA_STORE_NAME }}
#           PAYDUNYA_STORE_TAGLINE: ${{ secrets.PAYDUNYA_STORE_TAGLINE_STAGING || secrets.PAYDUNYA_STORE_TAGLINE }}
#           PAYDUNYA_STORE_PHONE: ${{ secrets.PAYDUNYA_STORE_PHONE_STAGING || secrets.PAYDUNYA_STORE_PHONE }}
#           PAYDUNYA_STORE_ADDRESS: ${{ secrets.PAYDUNYA_STORE_ADDRESS_STAGING || secrets.PAYDUNYA_STORE_ADDRESS }}
#           PAYDUNYA_STORE_URL: ${{ secrets.PAYDUNYA_STORE_URL_STAGING || secrets.PAYDUNYA_STORE_URL }}
#           PAYDUNYA_STORE_LOGO: ${{ secrets.PAYDUNYA_STORE_LOGO_STAGING || secrets.PAYDUNYA_STORE_LOGO }}
#         run: |
#           # Ensure Postgres accessory is up then deploy with staging config
#           if bin/kamal -c config/deploy.staging.yml app details 2>/dev/null; then
#             bin/kamal -c config/deploy.staging.yml accessory start postgres || true
#             bin/kamal -c config/deploy.staging.yml deploy
#           else
#             bin/kamal -c config/deploy.staging.yml setup
#             bin/kamal -c config/deploy.staging.yml accessory boot postgres || bin/kamal -c config/deploy.staging.yml accessory start postgres || true
#           fi

#       - name: Generate Sitemap
#         env:
#           SITEMAP_HOST: http://aa.okemamy.com
#         run: |
#           echo "Generating sitemap for staging..."
#           bin/kamal -c config/deploy.staging.yml app exec 'bundle exec rake sitemap:refresh:no_ping' || echo "Sitemap generation failed, continuing..."

