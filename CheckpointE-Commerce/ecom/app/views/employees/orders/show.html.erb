<% content_for :active_tab, 'commandes' %>

<div class="flex flex-col min-h-screen bg-gray-50">
  <main class="flex-1 px-4 sm:px-6 lg:px-12 xl:px-28 py-6">
    <%= render "shared/orders/show",
        back_to_orders_path: employees_orders_path(shop_id: @current_shop&.id),
        update_item_delivery_status_path: (@order ? update_item_delivery_status_employees_order_path(@order) : "#") %>
  </main>
</div>
