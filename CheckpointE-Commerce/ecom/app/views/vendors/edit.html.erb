<% content_for :title, "Editing vendor" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing vendor</h1>

  <%= render "form", vendor: @vendor %>

  <%= link_to "Show this vendor", @vendor, class: "w-full sm:w-auto ..." %>
  <%= link_to "Back to vendors", vendors_path, class: "w-full sm:w-auto ..." %>
</div>
