<% content_for :active_tab, 'clients' %>

<div class="flex flex-col min-h-screen bg-gray-50">
  <main class="flex-1 px-4 sm:px-6 lg:px-12 xl:px-28 py-6">
    <%= render "shared/clients/show",
        back_to_clients_path: employees_clients_path(shop_id: @current_shop&.id),
        order_path_for:       ->(order) { employees_order_path(order, shop_id: @current_shop&.id) } %>
  </main>
</div>
