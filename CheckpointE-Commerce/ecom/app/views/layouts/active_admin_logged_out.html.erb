<!doctype html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for?(:title) ? content_for(:title) : (respond_to?(:html_head_site_title) ? html_head_site_title : "aa Admin — Connexion") %></title>
    <%= render "active_admin/html_head" %>
    <%# Analytics Scripts %>
    <%= render "shared/analytics" %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>
  <body class="bg-gray-50 dark:bg-gray-900 text-gray-950 dark:text-gray-100 antialiased">
    <div class="mx-auto flex min-h-screen flex-col items-center justify-center px-4 py-4 sm:px-6 sm:py-8">
      <%= render "active_admin/flash_messages" %>
      <%= yield %>
    </div>
  </body>
</html>
