<%# Tailwind global (classes utilitaires utilisées dans app/admin/*.rb) %>
<% begin %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
<% rescue Propshaft::MissingAssetError, Sprockets::Rails::Helper::AssetNotFound %>
<% end %>

<%= stylesheet_link_tag "active_admin", "data-turbo-track": "reload" %>
<%= stylesheet_link_tag "active_admin_overrides", "data-turbo-track": "reload" %>
<%= stylesheet_link_tag "lexxy", "data-turbo-track": "reload" %>
<meta name="viewport" content="width=device-width,initial-scale=1">
<%= csrf_meta_tags %>
<%= csp_meta_tag %>
<% # On page load or when changing themes, best to add inline in `head` to avoid FOUC %>
<%= javascript_tag nonce: true do %>
  if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    document.documentElement.classList.add('dark')
  } else {
    document.documentElement.classList.remove('dark')
  }
<% end %>
<%= javascript_importmap_tags "active_admin", importmap: ActiveAdmin.importmap %>
<%= javascript_import_module_tag "lexxy" %>
